<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.sky.mapper.EmployeeMapper">
    <select id="getByUsername" parameterType="String" resultType="com.sky.entity.Employee">
        select id, name, username, password, phone, sex, id_number as idNumber, create_time as createTime,
               update_time as updateTime, create_user as createUser, update_user as updateUser, status
        from employee
        where username = #{username}
    </select>

    <insert id="insert" parameterType="com.sky.entity.Employee" useGeneratedKeys="true" keyProperty="id" keyColumn="id">
        insert into employee
        (name, username, password, phone, sex, id_number, create_time, update_time, create_user, update_user, status)
        values
        (#{name}, #{username}, #{password}, #{phone}, #{sex}, #{idNumber}, #{createTime}, #{updateTime}, #{createUser}, #{updateUser}, #{status})
    </insert>
</mapper>
